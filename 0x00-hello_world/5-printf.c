#include <stdio.h>

/**
 * main - Entry Point
 * @void: null value
 * Return: outputs 0 because of int function
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
